# Binary Search

**Time Complexity**: O(log n)
**Space Complexity**: O(1)

Classic divide and conquer algorithm. Frequently asked with variations (rotated sorted array, etc.).

**Interview Tips**: Always handle mid calculation carefully to avoid integer overflow.